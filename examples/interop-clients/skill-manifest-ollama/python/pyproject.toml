[project]
name = "turul-a2a-interop-client-skill-manifest-ollama"
version = "0.1.0"
description = "Python A2A client for the skill-manifest-ollama-agent example."
requires-python = ">=3.10"
dependencies = [
    "a2a-sdk==1.0.2",
]

[tool.uv]
package = false
