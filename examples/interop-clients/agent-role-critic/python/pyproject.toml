[project]
name = "turul-a2a-interop-client-agent-role-critic"
version = "0.1.0"
description = "Python A2A client for the agent-role-critic-agent example."
requires-python = ">=3.10"
dependencies = [
    "a2a-sdk==1.0.2",
]

[tool.uv]
package = false
