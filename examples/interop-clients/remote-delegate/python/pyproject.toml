[project]
name = "turul-a2a-interop-client-remote-delegate"
version = "0.1.0"
description = "Python A2A client for the remote-delegate-agent example."
requires-python = ">=3.10"
dependencies = [
    "a2a-sdk==1.0.2",
    "httpx",
]

[tool.uv]
package = false
